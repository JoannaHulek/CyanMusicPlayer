<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joannahulek.cyanmusicplayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainMenuActivity"
            android:label="CYAN MUSIC PLAYER">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NowPlayingActivity"
            android:label="NOW PLAYING" />
        <activity
            android:name=".SearchActivity"
            android:label="SEARCH" />
        <activity
            android:name=".AlbumDetailsActivity"
            android:label="ALBUM DETAILS" />
        <activity
            android:name=".MyAlbumsActivity"
            android:label="MY ALBUMS" />

        <activity android:name=".BuyNewAlbumActivity"
            android:label="BUY NEW ALBUM"/>
    </application>

</manifest>